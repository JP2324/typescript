console.log("this is typescript still");
export {};
// tsc -w watches over the file changes and compiles automatically
// tsc --init creates tsconfig.json file
// tsc compiles the ts files based on the configurations mentioned in tsconfig.json
